import { Theme } from "./styled";
import themeColor from "./themes/themeColor";
import themeFont from "./themes/themeFont";

const theme: Theme = {
    colors: themeColor,
    fonts: themeFont
}

export default theme;